# ============================================
# Photo Organizer - Environment Configuration
# Copy this file to .env and customize
# ============================================

# ----------------------------------------
# Server Configuration
# ----------------------------------------
HOST=0.0.0.0
PORT=8000

# API host port (for Docker)
HOST_PORT=8000

# Log level (debug, info, warning, error)
LOG_LEVEL=info

# Number of Uvicorn workers (keep at 1 for GPU)
WORKERS=1

# ----------------------------------------
# GPU Configuration
# ----------------------------------------
# Set to the GPU device index to use (0, 1, 2, etc.)
CUDA_VISIBLE_DEVICES=0

# Enable/disable GPU acceleration
USE_GPU=true

# Number of GPUs to reserve (for Docker)
GPU_COUNT=1

# ----------------------------------------
# Model Configuration
# ----------------------------------------
# Available models: resnet18, resnet50, resnet101, resnet152
MODEL_NAME=resnet152

# Batch size for inference (adjust based on GPU memory)
BATCH_SIZE=8

# Minimum confidence threshold for predictions
CONFIDENCE_THRESHOLD=0.1

# Number of top predictions to return
TOP_K_PREDICTIONS=5

# ----------------------------------------
# API Configuration
# ----------------------------------------
# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ----------------------------------------
# Upload Configuration
# ----------------------------------------
# Maximum file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# ----------------------------------------
# Docker Resource Limits
# ----------------------------------------
# CPU limit (number of cores)
CPU_LIMIT=4

# Memory limit
MEMORY_LIMIT=8G

# ----------------------------------------
# Nginx Settings (if using reverse proxy)
# ----------------------------------------
NGINX_PORT=80
NGINX_SSL_PORT=443

# ----------------------------------------
# Docker Image Settings
# ----------------------------------------
# Image tag for builds
TAG=latest
